spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/restaurant_db
    username: restaurant
    password: restaurant123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true